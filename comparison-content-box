.comp {
  height: auto; /* Adjust height as necessary */
  padding: 20px; /* Add padding for spacing */
  background: rgba(0, 0, 0, 0.6); /* Slightly transparent background */
  color: #f8f9fa; /* Light gray text for better readability */
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  margin: 20px auto; /* Centered content with automatic margins */
  border-radius: 10px; /* Rounded corners for a polished look */
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3); /* Subtle shadow for depth */
}



#programStatsChart {
  max-width: 800px;
  max-height: 500px;
  margin: 0 auto;
  padding: 10px;
  background: #ffffff; /* White background to make chart stand out */
  border: 1px solid #ddd;
  border-radius: 5px;
}

.comp::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5); /* Lighter overlay for readability */
  z-index: -1;
}

select.form-select {
  font-size: 1rem;
  padding: 10px;
  border-radius: 5px;
}

label.form-label {
  font-weight: bold;
  color: #ffffff;
}
